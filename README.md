# SQL-STUDY-GROUP-HANDS-ON-1
This repo holds the the files of a SQL analysis which focus on Banking System data. The project explore the creation of a database schema both on the logical model and physical model.
